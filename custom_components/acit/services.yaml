check_update:
  name: Check for updates
  description: Manually check whether an OTA update is available
  fields:
    device_id:
      name: Device
      description: The ACIT device to check
      required: true
      selector:
        device:
          integration: acit

